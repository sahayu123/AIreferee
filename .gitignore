# Python
__pycache__/
*.pyc

# Dedicated MediaPipe venv (Python 3.12) -- rebuild via requirements-test.txt
.venv-mediapipe/

# Claude Code local session config/permissions
.claude/

# Trained model artifacts / caches -- regenerate via train_foul_rf.py +
# extract_pose_features.py rather than tracking large binaries in git
# (finetuned_backbone.pt alone exceeds GitHub's 100MB file limit)
*.pt
*.pkl
embeddings_cache.npz
pose_features_cache.npz
pose_features_cache.partial.npz

# Auto-downloaded MediaPipe model assets
mediapipe_models/

# Rendered annotated-video output (grows every predict_foul.py run)
rendered_videos/

# Training run logs
*.log
